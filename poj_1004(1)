#include<iostream>

using namespace std;

int main(void){

double arr[12];
double result = 0.00;
for(int i = 0; i < 12; i++){
  cin >> arr[i];
  result += arr[i];
}
result = result / 12.0;
cout << "$" << result;


return 0;
}